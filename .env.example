# 环境变量配置

# 管理员密码（两种方式任选其一）
# 方式1：使用密码哈希（推荐，更安全）
# 使用 `node scripts/hashPassword.js your-password` 生成哈希
ADMIN_PASSWORD_HASH=your-password-hash-here

# 方式2：使用明文密码（不推荐生产环境，仅用于开发测试）
# ADMIN_PASSWORD=your-secure-password

# 可选：Webhook API 密钥（用于保护邮件接收端点）
# WEBHOOK_API_KEY=your-webhook-api-key

# 可选：清理过期邮件的令牌
# CLEANUP_TOKEN=your-cleanup-token

# 数据库路径（默认在项目根目录）
# DATABASE_PATH=./emails.db

# 调试模式
DEBUG=false
